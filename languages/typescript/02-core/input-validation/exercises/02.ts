function runInputValidationExercise(): void {
    // TODO 1: Read or prepare score count in range `1..50`, followed by that many scores in range
    //         `0..100`.
    // TODO 2: Validate a score count and matching scores, then print the average.
    // TODO 3: Produce average score; verify missing scores; invalid score values; boundary values
    //         `0` and `100`.
}

runInputValidationExercise();

export {};
