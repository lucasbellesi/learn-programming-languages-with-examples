function runSortingSearchingExercise(): void {
    // TODO 1: Read or prepare integer `n`, then `n` integers.
    // TODO 2: Sort integers ascending.
    // TODO 3: Produce the sorted values in ascending order; verify repeated values; negative
    //         values; `n <= 0`.
}

runSortingSearchingExercise();

export {};
