function runStructsClassesExercise(): void {
    // TODO 1: Read or prepare one integer `n`, then `n` pairs of `name score`.
    // TODO 2: Parse records into typed objects and print a summary.
    // TODO 3: Produce one typed summary line per record; verify `n <= 0`; missing tokens; invalid
    //         scores.
}

runStructsClassesExercise();

export {};
