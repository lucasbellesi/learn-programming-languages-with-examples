function runCopyMoveSemanticsExercise(): void {
    // TODO 1: Read or prepare item name, quantity, and reserved quantity.
    // TODO 2: Write a deep-clone helper for a small inventory model.
    // TODO 3: Produce original and cloned inventory states after a clone-only update; verify
    //         invalid counts; negative values.
}

runCopyMoveSemanticsExercise();

export {};
