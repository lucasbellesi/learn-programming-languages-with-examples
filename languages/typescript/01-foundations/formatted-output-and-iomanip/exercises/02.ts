function runFormattedOutputIomanipExercise(): void {
    // TODO 1: Read or prepare name and score pairs on one line.
    // TODO 2: Print a two-column leaderboard row with padded names and scores.
    // TODO 3: Produce one formatted row per pair; verify odd token counts should print an error;
    //         decimal scores should still align.
}

runFormattedOutputIomanipExercise();

export {};
