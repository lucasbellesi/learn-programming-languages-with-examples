function runStringsExercise(): void {
    // TODO 1: Read or prepare one token shaped like name@domain.
    // TODO 2: Normalize an email-like token to lowercase and mask the domain.
    // TODO 3: Produce the normalized token with the domain replaced by ***; verify missing @ should
    //         print an error; uppercase letters should be normalized.
}

runStringsExercise();

export {};
