function runControlFlowExercise(): void {
    // TODO 1: Read or prepare one non-negative integer.
    // TODO 2: Print a countdown and classify the last number reached.
    // TODO 3: Produce a countdown and a final classification line; verify 0 should still print one
    //         line; negative input should print an error.
}

runControlFlowExercise();

export {};
