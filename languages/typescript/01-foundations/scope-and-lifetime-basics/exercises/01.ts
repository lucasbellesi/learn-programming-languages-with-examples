function runScopeLifetimeBasicsExercise(): void {
    // TODO 1: Read or prepare one integer start value and one integer number of calls.
    // TODO 2: Build a closure-based counter that prints the next value on each call.
    // TODO 3: Produce the counter values in order; verify zero calls should print nothing; negative
    //         start values should still work.
}

runScopeLifetimeBasicsExercise();

export {};
