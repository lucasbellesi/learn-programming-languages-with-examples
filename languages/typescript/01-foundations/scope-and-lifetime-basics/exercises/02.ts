function runScopeLifetimeBasicsExercise(): void {
    // TODO 1: Read or prepare one word and one replacement word.
    // TODO 2: Show how a nested block can safely shadow a variable name.
    // TODO 3: Produce outer and inner values to show the scopes stay separate; verify empty words
    //         should print an error; identical words should still demonstrate two scopes.
}

runScopeLifetimeBasicsExercise();

export {};
