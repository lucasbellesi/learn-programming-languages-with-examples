namespace {
void runInputValidationExercise() {
    // TODO 1: Read or prepare score count in `[1, 50]`, then scores in `[0, 100]`.
    // TODO 2: Read valid scores and compute average.
    // TODO 3: Produce class average; verify invalid types mid-sequence; boundary values `0`, `100`.
}
} // namespace

int main() {
    runInputValidationExercise();
    return 0;
}
