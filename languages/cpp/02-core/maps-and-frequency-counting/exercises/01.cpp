namespace {
void runMapsFrequencyCountingExercise() {
    // TODO 1: Read or prepare integer count `n`, then `n` integers (`0-9`).
    // TODO 2: Count digit frequencies.
    // TODO 3: Produce frequency per digit; verify missing digits should show count `0`; all values
    //         same digit.
}
} // namespace

int main() {
    runMapsFrequencyCountingExercise();
    return 0;
}
