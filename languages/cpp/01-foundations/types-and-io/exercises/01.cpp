namespace {
void runTypesIoExercise() {
    // TODO 1: Read or prepare integer `N` followed by `N` numeric values.
    // TODO 2: Read `N` numbers and compute sum, average, min, max.
    // TODO 3: Produce sum, average, minimum, maximum; verify `N <= 0` should exit with message; all
    //         equal values should produce equal min/max.
}
} // namespace

int main() {
    runTypesIoExercise();
    return 0;
}
