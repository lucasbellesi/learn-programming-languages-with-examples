namespace {
void runArraysVectorsExercise() {
    // TODO 1: Read or prepare integer `n`, then `n` integers.
    // TODO 2: Print user-provided integers in reverse order.
    // TODO 3: Produce values in reverse order; verify `n <= 0` should print a friendly message;
    //         repeated values should remain repeated in output.
}
} // namespace

int main() {
    runArraysVectorsExercise();
    return 0;
}
